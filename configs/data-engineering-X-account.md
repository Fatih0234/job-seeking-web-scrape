# Grok Deep Research Prompt (Python-first) You have a Python 3.12 REPL. Use it. ## Input I will paste a YAML taxonomy of data-engineering keywords. ## Goal Find and rank X (Twitter) accounts that consistently post about data engineering. ## Steps (must follow) 1. **In Python**, parse the YAML into groups -> keyword lists. Normalize (lowercase), dedupe, and remove ultra-generic tokens (e.g., “data”, “cloud”, “sql” alone). 2. **In Python**, generate **80 X search queries**: - For each taxonomy group, create **8–12** queries. - Each query uses **3–6 keywords** from that group + **1 anchor term** from: - ["data pipeline","etl","elt","airflow","dbt","spark","kafka","lakehouse","warehouse","batch","streaming","orchestration"] - Ensure queries are diverse (no keyword appears in >10 queries). 3. Use those queries to search X (public posts, **last 90 days**). Prefer **original posts** (not retweets-only). Collect **candidate accounts** + a few **example posts** per account. 4. **In Python**, deduplicate accounts and compute a score per account: - group_coverage = number of taxonomy groups matched by their posts (last 90 days) - keyword_hits = total keyword matches - quality_hits = matches for: - ["postmortem","benchmark","deep dive","thread","tutorial","case study","incident","cost","latency"] - Final score: - 3*group_coverage + 0.5*keyword_hits + 2*quality_hits - minus penalties for promo/spam/repost-only behavior 5. **Output** - Top **50** accounts overall - Plus Top **10** per category (taxonomy group) - For each account include: handle, display name, short “why”, top groups, and **2 example posts with dates** - Also output a **CSV-like table** (copy/pasteable) ## Important Don’t hallucinate. Every account must be supported by at least **2 example posts** found during search. --- ## Optional: Hidden-gems mode If possible, produce two ranked lists: - **(A) Big reach** - **(B) Hidden gems** (lower follower count but high group_coverage and high quality_hits) --- ## YAML Taxonomy Paste the YAML below this line: ```yaml # (paste your taxonomy here)